colorlog==6.12.0
homeassistant==2026.7.1
# Pre-install the frontend so `scripts/develop` works offline-ish; HA's runtime
# installer often times out on this ~114 MB wheel. Keep in sync with the
# homeassistant pin (see homeassistant/components/frontend/manifest.json).
home-assistant-frontend==20260624.4
pip>=26.1.2
pytest-homeassistant-custom-component==0.13.345
ruff==0.15.20